---
interface Props {
  title?: string;
  href?: string;
}

const { title, href } = Astro.props;
---

{href ? (
  <a href={href} class="card card--linked">
    {title && <h3>{title}</h3>}
    <slot />
  </a>
) : (
  <div class="card">
    {title && <h3>{title}</h3>}
    <slot />
  </div>
)}
